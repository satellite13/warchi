#!/bin/sh
# Sync Helm chart version from package.json before commit (optional hook).
# Enable: git config core.hooksPath .githooks
set -e
cd "$(git rev-parse --show-toplevel)" || exit 1
node scripts/sync-chart-version.mjs
git add charts/warchi/Chart.yaml charts/warchi/values.yaml 2>/dev/null || true
