apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "warchi.fullname" . }}-test-connection"
  labels:
    {{- include "warchi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "warchi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
